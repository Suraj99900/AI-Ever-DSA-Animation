"""
AI-EVER visualizer package.

Modules (implemented in Phase 2+):
    parser        — AST-based source parsing
    tracer        — sys.settrace() execution tracing engine
    memory        — variable / heap snapshot builder
    animation     — animation event stream builder
    ast_builder   — AST tree serialization for the frontend
    flowchart     — graphviz flowchart generation
    graph_builder — networkx control-flow / memory graphs
    executor      — sandboxed step executor (never raw exec())
"""
